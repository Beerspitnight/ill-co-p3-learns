# Dataset Placeholder

This folder is used to store the image-caption pairs JSON file used for tagging.

📂 `combined_pairs_sampled_for_gpt.json` should be placed here manually for local development.

🚫 This file is **not committed** to version control for privacy and IP reasons.

✅ Expected format: a list of dicts, each with `image_path`, `caption`, `id`, etc.
